import 'package:flutter/material.dart';

class Sick extends StatelessWidget {
  const Sick({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}